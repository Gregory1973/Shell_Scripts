# Shell_Scripts
Command-line tools in shell terminal scripts

## createScriptCommandFile
Script for macOS: creates and initializes shell script files

## createSSHKeys
Script for macOS: generates a random passphrase, creates an SSH key pair
protected by this passphrase, then saves everything to a text file.
